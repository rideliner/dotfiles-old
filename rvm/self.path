
zstyle -s ':ride:config:rvm' install mode

rvm_reload_flag=1

if [ "$mode" = "single" ]; then
  script="$HOME/.rvm/scripts/rvm"
else
  script="/usr/local/rvm/scripts/rvm"
fi

[[ -s "$script" ]] && source "$script" 1>/dev/null
