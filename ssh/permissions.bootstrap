#!/usr/bin/env zsh

function {
  if [[ `stat -c '%a' $DOTFILES_PATH/ssh/config.symlink` == '640' ]]; then
    success 'permissions already set'
  else
    chmod 0640 $DOTFILES_PATH/ssh/config.symlink
    status $? 'fixed permissions to 0640'
  fi
}
