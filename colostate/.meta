if [[ $UID != 0 ]]; then
  case $DOTFILES_FULL_HOST in
    hastings* | marple* | *.cs.colostate.edu )
      zstyle ':ride:colostate' enabled true
  esac
fi

zstyle ':ride:colostate' depend ssh

zstyle ':ride:colostate:symlink:self' name '.!'
