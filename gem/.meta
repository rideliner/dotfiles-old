zstyle ':ride:gem' privileged false
zstyle ':ride:gem' groups dev
zstyle ':ride:gem' depend ruby

zstyle ':ride:gem:symlink:rc' name '.!?'

zstyle ':ride:gem:symlink:rubygems' path '~/.!'
zstyle ':ride:gem:symlink:rubygems' name 'credentials'

zstyle ':ride:gem:permissions:rubygems' file './rubygems.symlink'
zstyle ':ride:gem:permissions:rubygems' mode 600
