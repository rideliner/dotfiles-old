#!/usr/bin/env zsh

function {
  if (( $+commands[c_rehash] )) ; then
    c_rehash "$DOTFILES_PATH/ssl" >/dev/null
  elif (( $+commands[cacertdir_rehash] )); then
    cacertdir_rehash "$DOTFILES_PATH/ssl" >/dev/null
  else
    fail 'No command found to hash ssl certificates'
    return $?
  fi
}
